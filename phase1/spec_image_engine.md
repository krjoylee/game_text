# AsciiArt Studio — 이미지/아트 생성 엔진 기술 스펙 (Image Engine Spec)

> **프로젝트명**: AsciiArt Studio (아스키아트 스튜디오)  
> **정체성**: 고전문학 텍스트 게임을 위한 **이미지 ➔ 고밀도 아스키 아트 변환·저작 엔진**  
> **최종 갱신**: 2026-09-02  
> **관련 문서**: [philosophy.md](file:///home/krjoylee/code/game/philosophy.md) · [spec.md (게임 엔진 스펙)](file:///home/krjoylee/code/game/spec.md) · [todo.md](file:///home/krjoylee/code/game/todo.md)  

---

## 1. 프로젝트 목표 및 정체성

* **목표**: 손으로 그리기 어려운 고품질 일러스트, 명화, AI 생성 이미지를 **74 x 23 씬 카드 규격에 최적화된 초대형 고밀도 텍스트 아트**로 자동 변환하고 편집하는 저작 툴체인.
* **원칙**:
  1. **게임 엔진과의 완전한 분리**: 게임 엔진(`Divina Ludus`)은 런타임 렌더링에만 집중하고, 무거운 이미지 처리(OpenCV/PIL/NumPy)는 본 `AsciiArt Studio`에서 전담한다.
  2. **터미널 가독성 극대화**: 단순 픽셀-문자 치환을 넘어, 외곽선(Edge) 검출과 명암(Grayscale Ramp) 최적화를 통해 74컬럼 터미널에서도 형태를 한눈에 알아볼 수 있도록 한다.
  3. **고전 어드벤처 연출 문법 준수**: 대화/갈등 장면은 **좌우 대립 배치**, 독백/상징 장면은 **중앙 집중 배치** 구도를 자동 가이드한다.
  4. **YAML 스니펫 즉시 출력**: 변환 결과물을 복사해 `4_game_scene.yaml`의 `scene_art`에 즉시 붙여넣을 수 있는 포맷으로 제공한다.

---

## 2. 변환 파이프라인 (Conversion Pipeline)

```
[ 원본 이미지 (PNG/JPG/WebP) ]
               │
               ▼
┌────────────────────────────────────────┐
│ 1. 전처리 (Preprocessing)               │
│    • 종횡비 보정 (Aspect Ratio Adjust)  │ (터미널 폰트는 세로:가로 ≈ 2:1 비율 반영)
│    • 74 x 23 타겟 초대형 해상도 리사이징│
│    • 대비 향상 (CLAHE / 히스토그램 평활화)│
└──────────────────┬─────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────┐
│ 2. 다중 레이어 추출 (Dual Extraction)   │
│    • 레이어 A: Canny/Sobel 엣지(외곽선) │ ➔ `|`, `/`, `-`, `\`, `_`
│    • 레이어 B: 8단계 명암 램프 (Grayscale)│ ➔ `█`, `▓`, `▒`, `░`, `*`, `.`, ` `
└──────────────────┬─────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────┐
│ 3. 합성 및 텍스트 후처리 (Composite)    │
│    • 엣지 우선 합성 (외곽선을 선명하게 살림)│
│    • 좌우/중앙 인물 구도 프레이밍      │
│    • 74컬럼 x 23줄 너비 패딩 강제 고정│
└──────────────────┬─────────────────────┘
                   │
                   ▼
[ YAML 씬 아트 스니펫 (`scene_art: [ ... ]`, 23줄) ]
```

---

## 3. 핵심 모듈 구성

| 모듈 | 기술 스택 | 책임 및 역할 |
|------|-----------|--------------|
| **`preprocessor.py`** | OpenCV (`cv2`), Pillow | 이미지 로드, 회전, 2:1 폰트 비율 보정, 74x23 리사이징, 명암비 강화 |
| **`edge_detector.py`** | OpenCV (`cv2.Canny`) | 주요 피사체의 윤곽선을 검출하여 방향성 아스키 선(`|`, `-`, `/`, `\`)으로 변환 |
| **`shade_mapper.py`** | NumPy | 0~255 밝기 구간을 밀도 램프(`█▓▒░:· `)로 매핑 |
| **`yaml_exporter.py`** | PyYAML / Python Str | 14줄 고정 문자열 배열 포맷 및 YAML 파일 내보내기 |
| **`cli.py`** | Click / Argparse | 터미널 CLI 도구 (`ascii-studio convert <image> --contrast 1.2`) |
| **`web_preview.py`** (선택) | Streamlit / Flask | 슬라이더로 대비/엣지 임계값을 실시간 조절하며 미리보는 웹 UI |

---

---

## 4. 캐릭터 외형 스펙 체계 (Character Visual Specification)

일관된 고품질 인물 흉상을 위해 각 팩별로 **캐릭터 외형 시트(Character Sheet)**를 표준 스펙으로 정의합니다:

```yaml
# 캐릭터 외형 스펙 예시 (팩 메타데이터 연동)
characters:
  nolbu:
    name: "놀부"
    symbol: "凸"
    headwear: "검은 양반 갓 (넓은 챙)"
    facial: "치켜뜬 세모 눈, 매부리코, 삐죽 나온 입술, 뾰족한 턱수염"
    outfit: "비단 도포 (화려함과 탐욕)"
    default_pose: "좌측 배치 (오른쪽을 향해 호통치는 시선)"

  heungbu:
    name: "흥부"
    symbol: "♧"
    headwear: "찢어진 낡은 패랭이 모자"
    facial: "처진 눈망울, 눈물 자국, 순박하고 온화한 미소"
    outfit: "누더기 삼베옷"
    default_pose: "우측 배치 (왼쪽을 향해 공손히 고개 숙인 시선)"
```

---

## 5. 3대 렌더링 모드 규격

| 모드 (`--format`) | 서브픽셀 분할 | 유효 해상도 | 특징 및 용도 |
|:---|:---:|:---:|:---|
| **`braille` (추천)** | **2 x 4 서브픽셀** | **148 x 92 픽셀** | **8배 초고해상도 도트**: 눈동자, 눈물, 갓 끈, 표정 등 1980년대 PC-98 도트 명작 화질 완벽 구현 (1칸 고정 유니코드 U+2800..=U+28FF) |
| **`halfblock`** | **1 x 2 서브픽셀** | **74 x 46 도트** | **상하 2배 확장**: 굵고 선명한 레트로 아케이드 스타일 (`▀`, `▄`, `█`, ` `) |
| **`block`** | 1 x 1 일반 | 74 x 23 글자 | 8단계 명암 램프(`█▓▒░`) 기반 클래식 텍스트 아트 |

---

## 6. 향후 로드맵 (Roadmap)

* **v0.1**: Python CLI 기반 8배 Braille / Half-Block 서브픽셀 변환 엔진 구현 완료
* **v0.2**: 캐릭터 외형 시트(Character Sheet) 기반 프롬프트/스프라이트 자동 합성기
* **v0.3**: Streamlit 기반 실시간 슬라이더 조절 & YAML 복사 웹 GUI 제공
* **v0.4**: DALL-E / Stable Diffusion API 연동으로 "프롬프트 입력 ➔ 씬 아스키 아트 즉시 생성" 자동화
